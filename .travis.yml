language: generic
sudo: required
services: 
   - docker

before_install:
  - docker build -t bkuhlen73/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bkuhlen73/docker-react npm run test